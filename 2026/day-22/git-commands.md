################ GIT Commands ##########################

#git --version >> to check git version

#git init      >> to make a directory as git repository

#git status    >> To check git status

#git rm --cached <file_name>        >> To remove file from atage area

#git add <file_name>                >> To move file to stage area file

#git commit -m <commit_message>     >> all the changes will tracked for the file once commit is done.

#git log    >> To check all logs

#To configure globally "user name"
#git config --global user.name "user_name"     


#To Configure globally "user email"
#git config --global "abc@example.com"


#if file accidentaly deleted to restore file
#git restore <file_name>

#after using git add command if you wish to unstage file use this command
#git restore --staged <file>"

#To see all branch
#git branch

#git checkout -b dev ( will create new branch also show all logs) 

 #git checkout branch_name (to move on branch to another branch)

#git switch branch_name

#git branch -D test (to delete branch)

#To clone repo from git hub
#git clone url (copy url from git hub and paste)

#git remote -v (to check GitHub remote)
origin  https://github.com/suman-saha789/my_new_repo.git (fetch)
origin  https://github.com/suman-saha789/my_new_repo.git (push)

#git remote set-url origin git@github.com:suman-saha789/my_new_repo.git ( to change origin from https to ssh)



